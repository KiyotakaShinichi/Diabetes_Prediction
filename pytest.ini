[pytest]
testpaths = tests
addopts = -ra --strict-markers --strict-config
# No global warning filters: logisticregression_only / boostedtrees_ab already
# call warnings.filterwarnings("ignore") at import time, and tests/test_training_metrics.py
# undoes that so real warnings stay visible in the run summary.
