"""Track K: do modern deep tabular models beat strong classical baselines here?

The protocol is frozen in protocol.py and documented in
docs/research/track_k_protocol.md. Both were committed before any model was
evaluated on the held-out test set.
"""
